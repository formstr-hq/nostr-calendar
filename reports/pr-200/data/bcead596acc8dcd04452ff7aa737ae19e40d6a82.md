# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: event-crud.spec.ts >> user opens the app and creates a calendar event
- Location: e2e/web/tests/event-crud.spec.ts:8:1

# Error details

```
Error: expect(locator).not.toBeVisible() failed

Locator:  getByRole('dialog')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for getByRole('dialog')
    14 × locator resolved to <div role="dialog" aria-modal="true" aria-labelledby="_r_f_" class="MuiPaper-root MuiPaper-elevation MuiPaper-rounded MuiPaper-elevation24 MuiDialog-paper MuiDialog-paperScrollPaper MuiDialog-paperWidthMd MuiDialog-paperFullWidth css-jnxu76">…</div>
       - unexpected value "visible"

```

```yaml
- dialog "Create New Event":
  - heading "Create New Event" [level=2]
  - text: Title
  - textbox "event title":
    - /placeholder: Enter event title
  - textbox "Image URL eg. https://example.com/image.jpg"
  - separator
  - text: Start time
  - group "Start time"
  - text: "-"
  - group
  - separator
  - textbox "Enter location"
  - separator
  - text: Select recurrence pattern
  - combobox: Does not repeat
  - separator
  - heading "Notifications" [level=6]
  - text: Reminder
  - spinbutton "Reminder": "10"
  - combobox: minutes before
  - button "Remove"
  - text: Reminder
  - spinbutton "Reminder": "0"
  - combobox: minutes before
  - button "Remove"
  - button "Add reminder"
  - separator
  - textbox "Enter participant nPub"
  - button [disabled]
  - separator
  - paragraph: Forms
  - textbox "Paste form naddr or Formstr URL"
  - button "Add" [disabled]
  - separator
  - textbox "Add event description..."
  - text: Calendar
  - combobox: My Calendar
  - separator
  - paragraph: Event Type
  - button "Private"
  - checkbox "Show this time as busy on my public availability" [checked]
  - paragraph: Show this time as busy on my public availability
  - text: Publishes an event so others booking with you can see that this slot is taken. The entry contains only start/end times and no event details.
  - button
  - text: Publishing to 1 relay(s)
  - button "Cancel"
  - button "Save Event"
```

# Test source

```ts
  1  | import { TEST_PASSPHRASE } from "../../relay/seed/keys.js";
  2  | import { test, expect, navigate } from "../fixtures/index.js";
  3  | 
  4  | // Navigate to a fixed future date so the test is date-independent.
  5  | const TEST_DATE = "2027-01-15";
  6  | const [year, month, day] = TEST_DATE.split("-");
  7  | 
  8  | test("user opens the app and creates a calendar event", async ({ authedPage: page }) => {
  9  |   // Navigate to the day view for the test date
  10 |   await navigate(page, `/d/${year}/${month}/${day}`);
  11 | 
  12 |   // Click a time slot on the grid to open the create-event dialog.
  13 |   // The DayView renders 24 hour-cells, each with data-date. We click the
  14 |   // 10am slot (index 10) so the dialog pre-fills a reasonable start time.
  15 |   await page.locator(`[data-date="${TEST_DATE}"]`).nth(10).click();
  16 | 
  17 |   // Scope interactions inside the dialog
  18 |   const dialog = page.getByRole("dialog");
  19 |   await dialog.waitFor({ state: "visible" });
  20 | 
  21 |   // Fill in the event title
  22 |   await dialog.getByPlaceholder("Enter event title").fill("Team Standup");
  23 | 
  24 |   // Open the calendar selector and create a new calendar (required to enable Save).
  25 |   const calendarSelect = dialog.getByTestId("calendar-list-select");
  26 |   await calendarSelect.scrollIntoViewIfNeeded();
  27 |   await calendarSelect.click();
  28 |   await page.getByRole("option", { name: "Add new calendar" }).click();
  29 | 
  30 |   // CalendarManageDialog opens — fill the name and confirm.
  31 |   const calendarDialog = page.getByRole("dialog", { name: "New Calendar" });
  32 |   await calendarDialog.waitFor({ state: "visible" });
  33 |   await calendarDialog.getByLabel("Calendar Name").fill("My Calendar");
  34 |   await calendarDialog.getByRole("button", { name: "Create" }).click();
  35 |   await calendarDialog.waitFor({ state: "hidden" });
  36 | 
  37 |   // Save — button text comes from the "event.saveEvent" i18n key ("Save Event")
  38 |   await dialog.getByRole("button", { name: "Save Event" }).click();
  39 | 
  40 |   // The dialog closes and the new event appears on the calendar grid
> 41 |   await expect(dialog).not.toBeVisible();
     |                            ^ Error: expect(locator).not.toBeVisible() failed
  42 |   await expect(page.getByText("Team Standup")).toBeVisible();
  43 | });
  44 | 
```