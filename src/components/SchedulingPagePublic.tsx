import { Box, Typography } from "@mui/material";

/**
 * Placeholder for public scheduling page (booking view).
 * Will be fully implemented in a subsequent commit.
 */
export const SchedulingPagePublic = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Book an Appointment</Typography>
      <Typography color="text.secondary">Coming soon...</Typography>
    </Box>
  );
};
