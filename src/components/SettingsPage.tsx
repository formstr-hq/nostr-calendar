import { Box, Typography } from "@mui/material";

/** Placeholder — real content ships in F-SET (docs/REDESIGN_MASTER_PLAN.md). */
export function SettingsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Settings</Typography>
    </Box>
  );
}
